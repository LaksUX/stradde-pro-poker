/// <reference types="vite/client" />

// Injected via vite.config.ts's `define` — package.json's version plus a
// build counter (total commit count), so it's a new value every build with
// no manual bumping.
declare const __APP_VERSION__: string
